Expo SDK 57 with Dev Client (required for native ML modules), expo-router for file-based navigation, NativeWind v4 + Tailwind CSS for styling, Zustand for client state, TanStack Query for sync orchestration, Drizzle ORM over expo-sqlite for offline-first local database, react-native-fast-tflite running a converted INT8-quantized H-CBM model, react-native-vision-camera for frame processing, Supabase (Postgres + Storage + Auth) as remote backend, expo-task-manager + expo-background-fetch + @react-native-community/netinfo for background sync, react-hook-form + zod for validation, i18next + react-i18next for multi-language support, and expo-secure-store for auth tokens/PIN.