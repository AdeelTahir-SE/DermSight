This is the project root that wires together the build, runtime, and deployment configuration for an Expo SDK 57 application. `package.json` declares `expo-router/entry` as the main module and pins all Expo SDK 57 dependencies (camera, location, secure-store, sqlite, task-manager, background-fetch, vision-camera, fast-tflite, supabase-js, drizzle-orm, zustand, tanstack-query, i18next, nativewind). `app.json` defines the Expo manifest: bundle ID `com.dermsight.app`, portrait orientation, deep-link scheme `dermsight`, splash screen, Android adaptive icons with the medical teal palette (`#0D9E94`), declared native permissions (camera, fine/coarse location, storage), and config plugins for `expo-router`, `expo-splash-screen`, `expo-location`, and `expo-camera`. The build pipeline chains `metro.config.js` → `babel.config.js` (using `babel-preset-expo` with `jsxImportSource: nativewind`) → `tailwind.config.js` (medical-themed color tokens) → `global.css` (NativeWind entry). TypeScript path aliases `@/*` → `./src/*` are configured in `tsconfig.json`. `ARCHITECTURE.md` documents the layered design: UI screens under `app/` consume Zustand stores and feature hooks from `src/features/`, which read/write local SQLite via Drizzle ORM (`src/db/`), trigger an out-of-process sync engine to Supabase, and run on-device TFLite inference over a quantized H-CBM model bundled under `src/ml/assets/`. `assets/locales/{en,fr,sw}.json` provide i18n bundles; `assets/icons`, `assets/images`, `assets/splash`, and `assets/expo.icon` supply platform-specific iconography. A `scripts/reset-project.js` helper scaffolds a blank `src/app` layout.