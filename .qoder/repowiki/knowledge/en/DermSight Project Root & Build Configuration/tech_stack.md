Expo SDK 57 with Dev Client (required for native modules like `react-native-fast-tflite` and `expo-camera`), expo-router for file-based routing, NativeWind v4 + Tailwind CSS v3 for styling, TypeScript 6 with strict mode, and a dependency set centered on Drizzle ORM over expo-sqlite, Supabase JS client, Zustand stores, TanStack Query, i18next/react-i18next, react-hook-form + zod, and expo-task-manager/background-fetch for background sync.